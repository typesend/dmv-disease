const breadcrumbs = require('./extracts/breadcrumbs');
const messages = require('./extracts/messages');
const form = require('./extracts/form');
const footer = require('./extracts/footer');
const activeElement = require('./extracts/activeElement');

module.exports = {
  breadcrumbs,
  messages,
  form,
  footer,
  activeElement
};
